export type QuizAnswers = {
  environment?: string;
  energy?: string;
  social?: string;
  noise?: string;
  time?: string;
  allergies?: string;
};

export type PetMatch = {
  name: string;
  explanation: string;
  challenge: string;
  careTip: string;
};
